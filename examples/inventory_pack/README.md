# Inventory Capability Package

This package is intentionally unrelated to the reference Host. It proves that
a business package can be installed through the public Extension SDK without
modifying Core.

Use `INVENTORY_MANIFEST` as the declaration, install
`InventoryCapabilityPack`, and provide evaluation cases tagged with all
required certification scenarios. See `tests/test_inventory_example.py`.
